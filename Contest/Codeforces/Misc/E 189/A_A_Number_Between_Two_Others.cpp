#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define fast ios::sync_with_stdio(false); cin.tie(NULL)
#define all(x) (x).begin(), (x).end()

bool hcf_1(a,b) {
    if(a<b) return hcf_1(b,a) ;
    ll i = 1 ;
    ll max = 1
    for(; i <= b , i*i <= a ; i++) {
        if()
    }
}
void solve() {
    
}

int main(){
    fast;
    int t = 1;
    cin >> t;
    while(t--) solve();
    return 0;
}