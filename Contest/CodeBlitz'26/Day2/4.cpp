#include <iostream>
using namespace std;

int main() {
    int n , h ;
    cin >> n>>h;
    int arr[n] ;
    for(int i = 0 ; i < n ;i++) {
        cin>>arr[i];
    }
    int temp_h = h ;
    int max_diff = arr[n-1] - arr[0] ;
    for(int i = 0 ; i < n - 1; i++) {
        int diff = arr[i+1] - arr[i] ;
        temp_h -= diff ;
        if(min > diff)
        if(temp_h <= 0) {
            cout<<h-a[i]
        } 
    }
    int z = h-arr[n-1]+arr[0] ;

    cout<<<<endl;
}